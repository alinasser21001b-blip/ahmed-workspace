node_modules/
dist/
build/
coverage/
.turbo/
*.tsbuildinfo
.env
.env.local
.env.*.local
.expo/
.expo-shared/
web-build/
*.log
.DS_Store

# Local storage driver output — uploads must never be committed.
.storage/

# Netlify CLI/build state — never user code.
.netlify/
