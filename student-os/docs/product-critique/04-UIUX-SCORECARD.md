# Scorecard

8 = good enough for pilot. Every score < 8 carries its blocker and the
acceptance condition for reaching 8.

| Dimension | Score | Blocker (→ acceptance for 8) |
|---|---|---|
| ProductIdentity | 7 | Identity real but diluted by chrome (RC-01). → No scaffolding visible on any student screen. |
| VisualHierarchy | 7 | Strong on Today/Practice; collapses on sparse screens (RC-04). → Sparse compositions designed for Topics/Rooms/Conversation. |
| Typography | 7 | Faces correct; Topics subtitle/rule crowding; UA focus ring in pill; leading-ellipsis lines (RC-03/07). → Those three fixed, nothing else regresses. |
| Layout | 6 | 60–85 % voids; date line wraps (RC-02/04). → No screen reads majority-void with pilot data; context line one line at 390. |
| Navigation | 6 | Search only from Home; Topics/Learn overlap unexplained; Rooms grab-bag. → Search reachable where the need arises; one-line role statement per tab proven by content. |
| CoreLearningUX | 7 | Learn→Topic continuity missing (RC-06); resume band absent in fixture. → Survivor transition shipped; resume present with pilot data. |
| PracticeUX | 8 | — (banner removal assumed under RC-01; otherwise pilot-ready). |
| SocialUX | 6 | Rooms decorative at current density; group truncation bug. → RC-03 fixed; Rooms sparse design. |
| MessagingUX | 6 | Permanent negative first line; sparse thread (RC-05/02). → Notice redesigned (inline, once); thread density from pilot data. |
| ArabicRTL | 7 | Interface native; content half-English; bidi truncation (RC-02/03). → Arabic fixture content complete; truncation correct in both scripts. |
| Accessibility | 7 | Audits pass; web focus styles UA-default; dynamic type undeviced. → Designed focus states; one device pass recorded. |
| Motion | 6 | Only-on-load fades; sample 1 and tab feedback unshipped (RC-06). → Approved language perceptible in normal use; nothing added beyond it. |
| EmptyStates | 7 | True empties good; sparse ≠ empty unhandled (RC-04). → Sparse states designed. |
| MobileErgonomics | 7 | Targets/thumb fine; Practice footer scroll-under cue missing. → Cue added; nothing else. |
| WebPresentation | 6 | Canvas fixes stretch; keyboard/web affordances undesigned (RC-07). → Focus + hover states designed within tokens. |
| EmotionalQuality | 5 | Apologetic posture; skeletal density (RC-05/02/04). → One notice at a time, warm sparse states, full fixture. |
| StudentUsefulness | 5 | No daily-return spine beyond Practice resume; Topics=3 rows (RC-02 + product). → See 08-PRODUCT-USEFULNESS acceptance. |
| ProductionPolish | 5 | RC-01 wholesale; RC-03; focus ring. → Deletion list REMOVE_NOW executed; both defects fixed. |

**Average: 6.3** · Lowest: StudentUsefulness / EmotionalQuality /
ProductionPolish (5) · Highest: PracticeUX (8).
